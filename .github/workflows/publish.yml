name: '🚀 release'

on:
  push:
    branches: [master]

jobs:
  release:
    name: 🚀 release
    runs-on: ubuntu-latest
    steps:
      - name: 📚 checkout
        uses: actions/checkout@v2.1.1
      - name: 🟢 node
        uses: actions/setup-node@v2
        with:
          node-version: 14
          registry-url: https://registry.npmjs.org
      - name: 🚀 publish
        run: |
          npm i -g vsce
          vsce package
          vsce publish minor -p $AZURE_TOKEN
        env:
          AZURE_TOKEN: ${{secrets.AZURE_TOKEN}}